# Required for AI features (phishing email/phone/deepfake generation, analysis, chat).
# Get a free key at https://aistudio.google.com/apikey
# Copy this file to .env or .env.local and replace the placeholder with your key. Do not commit real keys.
GEMINI_API_KEY="your-gemini-api-key-here"

# Optional: used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"
